# Component: CLI (COMP-8)

**Status:** ACTIVE
**Description:** Command-line interface — all user-facing commands

**Layer:** interface

## Responsibilities

—

## Relationships

### Dependencies

| Target | Type |
|--------|------|
| Core (`COMP-1`) | depends-on |
| Pipeline (`COMP-2`) | depends-on |
| Manifest (`COMP-3`) | depends-on |
| Documentation (`COMP-4`) | depends-on |
| Orchestration (`COMP-5`) | depends-on |
| Authoring (`COMP-7`) | depends-on |

### Exposes Interfaces

- **CLI API** (`IF-auto-COMP-8`)

### Satisfies Requirements

- MCP tool compatibility (`REQ-26`)
