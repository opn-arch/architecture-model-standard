# Track 2 — Sitting B SE Panel Re-Review (2026-09-10)

## Context

After committing Track 1 fix (`5f88282` — stamp `source_file` on library-inferred behaviors), the viewer + SE docs were regenerated against the restored canonical `.architecture-model.yaml` (94 caps / 29 comps / 88 behaviors / 5 layers / 438 rels).

Baseline: `PYTHONPATH="$PWD/src" pytest tests/ -q --ignore=tests/test_config_loader.py` → **3638 passed / 104 skipped**.

Panels reviewed from `.architecture-models/docs/se/` regenerated by `architecture-model docs .` at 2026-09-11T00:10:38Z (model_hash `5eb5e40711f6`, edition 11).

## Verdict: 4 / 4 SE panels FAIL

Same defects as Sitting A. Track 1's duplicate-behavior fix eliminates the SoS-scope duplication but the review-critical defects are unchanged because they stem from **curation / projector-filter / model-completeness** gaps, not the pipeline extractor bug fixed in Track 1.

## Findings

### A1 ConOps — FAIL (11 diagnostics)

`conops.md:15` — 15 "curated operational scenarios" are actually raw AST-discovered behaviors:
- `BEH-1` GET , `BEH-10` GET login/, `BEH-11` GET logout/, `BEH-12` GET password_change/, `BEH-13` GET password_change/done/, `BEH-14` GET password_reset/, `BEH-15` GET password_reset/done/, `BEH-16` GET reset/<uidb64>/<token>/, `BEH-17` GET reset/done/, `BEH-18` GET <path:url>, `BEH-2` GET bookmarklets/ — **Django auth handlers, not project use cases**
- `BEH-19..BEH-22` CLI: Test Guided Round Trip / Test Enriched / Test Multi Repo / Test Round Trip — **test-module CLI helpers, not user-facing scenarios**

Every scenario has empty Goal / Systems / Inputs / Outputs / Interfaces / Requirements. Failure-mode section says "No failure modes defined" for all 15.

Line 23 diagnostics — 7 phantom IDs + 1 new duplicate:
- `Duplicate entity ID rejected in .architecture-model: SYS-src-(core)` — **new** since Sitting A
- Entity not found: `CAP-COMMENT-COLLABORATION`, `CAP-SIL-INSTRUMENTATION`, `CAP-LLM-ROUTING`, `COMP-COMMENT-SYS`, `COMP-SIL`, `COMP-LLM`, `COMP-DASHBOARD`
- 10 operational paths omitted (limit 15) — total pool is 25 behaviors → all noise

### A2 Functional Analysis — FAIL

`functional-analysis.md:22` "Capability Inventory and Decomposition" is empty.
`functional-analysis.md:24-27` "Functional Flows" table is empty.

Line 31 diagnostic: "hierarchy contains 6 raw capabilities. The curated overview represents **0 member capabilities in 7 functional blocks**; 6 capabilities are omitted."

- Model has 94 capabilities but projector sees only 6 top-level capabilities (L2 hierarchy dropped).
- 7 F-blocks resolve to zero members because F-block groups in `viewer-curation.yaml` reference the same 7 phantom IDs as A1.

### A3 Logical Architecture — FAIL

`logical-architecture.md:22-47` — all 5 layer tiers (Web / Application·Orchestration / Domain·Service / Data·Contracts / Infrastructure) have **empty** "System / Aggregate" tables.

`logical-architecture.md:51` — "displays 0 of 0 relationships". Cross-System Dependency table empty.

Root causes:
- Layer→Component allocation missing in canonical model (5 layers exist but 0 `contains` / `allocated-to` edges).
- LLM + SI&L layers missing entirely (Track 3 additions).
- Same 7 phantom IDs polluting curation.

### A4 Use Cases — FAIL

`use-cases.md:24-26` Actor-Goal Matrix has 1 actor (API Consumer) with `— (no featured use cases)`.

`use-cases.md:28-178` Featured Use Case Catalog lists 10 UCs (`UC: GET`, `UC: GET login/`, `UC: GET logout/`, ...) — all raw HTTP handlers with every field empty (Goal —, Trigger —, Pre —, Post —, MoE —, Systems —, Steps —, Requirements —).

Line 182 diagnostic: "15 nonfeatured use cases are omitted from the primary catalog".

Missing (per Sitting A + Track 3 audit): 4 user-facing use cases — Slice-scoped-context, Learning-loop, Development-loop, Comment-driven-collaboration.

## Recurring Root Causes (Sitting A → Sitting B, unchanged)

1. **Phantom IDs in curation (7 confirmed):** `viewer-curation.yaml` references entities the canonical model never emits.
2. **Projector filter gap** (`src/architecture_model/core/se_view_projectors.py:1967`): all 88 behaviors including 76 pipeline sub-behaviors + Django auth handlers get bucketed as first-class operational scenarios / featured UCs.
3. **Model-completeness gap** (Track 3): Phase B entities (feedback / SI&L / LLM / dashboard / comment components + LLM/SIL layers + 4 user-facing use-case behaviors) are missing from the canonical model.
4. **NEW: duplicate `SYS-src-(core)` rejected** during model load — investigate whether this is Track 1-adjacent or a separate synthesize-stage bug.

## Impact on Remaining Tracks

- **Track 3** (model completeness): must land **before** any re-review can pass. Adds the 47 pipeline-discovered entities from `.architecture/reviews/track3-audit-checklist.md` (feedback, SI&L, comments, profiles components; SIL/LLM layers; GateResult/ProjectSnapshot/ExportResult interfaces) plus the 4 user-facing use-case behaviors.
- **Track 4** (slice anchoring): schema addition to `viewer-curation.yaml` — each view declares `slice_ref: {entities, files, artifacts}`. Projectors read it to constrain scope and cite provenance. Design-then-implement.
- **Track 5** (FIX-A4.2 projector filter): at `se_view_projectors.py:1967`, filter behaviors by category (e.g. `role == "user_facing"` or `parent is None`) to stop leaking pipeline sub-behaviors + Django auth handlers into ConOps/Use Cases panels.
- **Curation cleanup:** either remove the 7 phantom IDs from `viewer-curation.yaml` OR let Track 3 introduce them into the canonical model. The audit checklist implies Track 3 should introduce them — but the curation IDs (`CAP-COMMENT-COLLABORATION`, `COMP-SIL`, `COMP-LLM`, `COMP-DASHBOARD`, ...) don't match the pipeline's IDs (`comp-3-12`, `comp-3-18`, `LAY-*`). Canonical IDs must be authored during Track 3.

## Dependency Order (unchanged)

1. ~~Track 1: duplicate-behaviors fix~~ **✓ committed `5f88282`**
2. Track 2: baseline review **✓ this document**
3. Track 3: model completeness (unblocks A1/A2/A3/A4)
4. Track 4: slice anchoring (adds provenance rigor to Track 3 additions)
5. Track 5: FIX-A4.2 projector filter (final polish)

## Artifacts

- Regenerated viewer: `.architecture/diagrams/viewer.html` (6.7 MB, 2026-09-10 20:00)
- Regenerated SE docs: `.architecture-models/docs/se/{conops,functional-analysis,logical-architecture,use-cases}.md`
- Track 3 audit checklist: `.architecture/reviews/track3-audit-checklist.md`
- Reference snapshots: `/tmp/track2-reconcile/{committed,regen}.yaml`
