comment is made on view, its saved, a log (issue) is created with comment id, for logs-db.
when requested to work on it MCP does slicing, requirements, and dev - then saves, commits with model diff & opencode session id, also close the issue in logs.
wheres the validator in this?
what else does the mcp do?

i want to see the pipeline (view)
requirements (intent, moe, etc) flow down to components and modules
how are each of these components logged and self-improved - all components in this flow for ams and opencode-arch
flow / pipeline for slicing, dev

self-improvement and looging layer - si&l


LLM provider layer
formalize
- mcp
- frontier - gpt / claude
- relay-ext