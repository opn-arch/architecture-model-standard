component: COMP-157
interfaces:
- id: IF-5
  name: COMP-157 Library API
  type: library
  methods:
  - 'apply_corrections: def apply_corrections(model: Any, corrections: list[dict])
    -> tuple[Any, list[str]]'
  - 'get_corrections_for_stage: def get_corrections_for_stage(ctx: PipelineContext,
    stage_name: str) -> list[Correction]'
  - 'load_corrections: def load_corrections(repo_path: Path) -> list[dict]'
  - 'mark_applied: def mark_applied(repo_path: Path, correction_ids: list[str]) ->
    None'
  - 'store_correction: def store_correction(repo_path: Path, correction: dict) ->
    dict'
